begin declare nat : natural,  
              str : string,           
              bool : boolean;
      nat := 1;
      str := "a";
      bool := true;
      nat := nat + 1;
      nat := nat - nat;
      str := str ||  "jan" || "aaa";
      bool := true == false;
      bool := bool;     
      bool := nat == 0;
      bool := nat != nat + 1;
      bool := nat + 1 - 2 != 9
end